---
// Small mono metadata voice: LS-S1-W02, JUL 19.
interface Props {
  text: string;
}

const { text } = Astro.props;
---

<span class="mono ticket">{text}</span>

<style>
  .ticket {
    color: var(--muted);
    font-size: 0.66rem;
  }
</style>