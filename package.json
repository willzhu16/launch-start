{
  "name": "launch-start",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.9.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "sync": "node --env-file-if-exists=.env scripts/sync-external.mjs",
    "cf:login": "wrangler login",
    "check": "astro check && biome check .",
    "lint": "biome check .",
    "typecheck": "astro check",
    "test": "vitest run",
    "test:dist": "vitest run --config vitest.dist.config.ts",
    "deploy:dry-run": "pnpm check && pnpm test && pnpm build && pnpm test:dist && wrangler deploy --dry-run",
    "deploy": "pnpm check && pnpm test && pnpm build && pnpm test:dist && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/mdx": "^7.0.8",
    "@astrojs/rss": "^4.0.19",
    "@astrojs/sitemap": "^3.7.4",
    "astro": "^7.3.2",
    "sharp": "^0.35.4"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@biomejs/biome": "^2.5.13",
    "@fontsource/fraunces": "^5.3.0",
    "@fontsource/jetbrains-mono": "^5.3.0",
    "@fontsource/source-serif-4": "^5.3.0",
    "@types/node": "^26.5.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.11",
    "wrangler": "^4.131.1"
  }
}
